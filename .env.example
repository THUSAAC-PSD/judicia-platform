# Judicia Platform Configuration

# Server Configuration
SERVER_ADDRESS=127.0.0.1:5000
JWT_SECRET=your-jwt-secret-key-change-in-production

# Database Configuration - Option 1: Direct PostgreSQL URL (Recommended for Supabase)
# Get this from your Supabase project -> Settings -> Database -> Connection string (URI)
DATABASE_URL=postgresql://postgres.your-project-id:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# Database Configuration - Option 2: Supabase Configuration (Alternative)
# Use these if you want to build the connection dynamically
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-from-project-settings
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-project-settings

# Message Queue Configuration
# For development, use local services or cloud alternatives like Upstash Redis
REDIS_URL=redis://localhost:6379
RABBITMQ_URL=amqp://localhost:5672

# Development Options
TEST_MODE=false

# Frontend Configuration (for Vite)
VITE_API_BASE_URL=http://127.0.0.1:5000/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-from-project-settings

# Legacy Node.js config (if needed)
PORT=5000
SESSION_SECRET=replace-this-in-production
SUPERADMIN_EMAIL=
SUPERADMIN_PASSWORD=
